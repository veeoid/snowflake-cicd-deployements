name: Deploy PRD

on:
  push:
    branches: [release/**]
  workflow_dispatch:

concurrency:
  group: deploy-prd
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: snowflake-prd # required-reviewer gate
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install snowflake-connector-python pyyaml

      - name: Generate PRD folder from TST
        run: python deploy/convert_env.py --from tst --to prd

      - name: Commit generated PRD folder
        run: |
          git config user.name  "convert-bot"
          git config user.email "convert-bot@users.noreply.github.com"
          git add MY_PROJECT_PREP_PRD MY_PROJECT_ANALYTICS_PRD
          if git diff --staged --quiet; then
            echo "No PRD changes"
          else
            git commit -m "Generate PRD folder from TST [skip-convert]"
            git push origin HEAD:${GITHUB_REF_NAME}
          fi

      - name: Deploy to PRD
        env:
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
        run: python deploy/deploy.py --env prd

      - name: Tag the release
        run: |
          TAG="v${GITHUB_REF_NAME#release/}"
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG exists"
          else
            git tag -a "$TAG" -m "Deployed to PRD from ${GITHUB_REF_NAME}"
            git push origin "$TAG"
          fi
